/// Placeholder Firestore service.
/// Initialize Firebase in main() and use this class to read/write progress, users, etc.
///
/// For demo this file is minimal. See firebase docs to wire up actual Firebase config.
class FirestoreService {
  static Future<void> initialize() async {
    // call Firebase.initializeApp();
  }

  Future<void> saveProgress(String userId, Map data) async {
    // implement saving to Firestore
  }
}
